version: '3.8'

services:
  orca:
    build:
      context: .
      dockerfile: Dockerfile
    image: orca-marine-platform:latest
    container_name: orca_platform
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - HOST=0.0.0.0
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
